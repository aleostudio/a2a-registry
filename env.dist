# App config (127.0.0.1 for localhost, 0.0.0.0 for whole network)
APP_NAME="A2A registry"
APP_VERSION="0.1.0"
APP_HOST=0.0.0.0
APP_PORT=9300

# Logging
DEBUG=False

# CORS (comma-separated origins, * for all)
CORS_ORIGINS=*

# A2A clients health check
HEALTH_CHECK_INTERVAL=30
MAX_FAILURES=3
